//Inside [id].tsx, use Next.js data fetching methods:

//getStaticPaths — to generate paths for all product IDs

//getStaticProps — to fetch product data for each product ID at build time

//Use Sanity client to fetch product details by the product _id